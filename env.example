# Enable client-side Google auto-translate to English (true/false)
NEXT_PUBLIC_ENABLE_GOOGLE_AUTO_TRANSLATE=false
# --- Owner Lock (removed) ---
# Owner lock has been deprecated and removed. No variables are required.

# Supabase/PostgreSQL Connection String
DATABASE_URL="postgresql://postgres.ezecvqmfasgldtjbhqud:Nalendramuhammad0104@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-for-development"

# Next.js Configuration
NEXTAUTH_URL="https://tpa-seven.vercel.app"
NEXTAUTH_SECRET="your-nextauth-secret-key" 

# Public Base URL used for links in emails (e.g., OTP verification)
# Example for local: http://localhost:3000
# Example for prod: https://tpa-seven.vercel.app
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# SMTP / Email configuration
# If using Gmail, set SMTP_USER to your Gmail address and SMTP_PASS to an App Password
# (Google Account -> Security -> 2-Step Verification -> App Passwords).
# Either set SMTP_HOST/SMTP_PORT for a custom SMTP server, or leave SMTP_HOST empty to use Gmail service automatically.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password-or-smtp-password"
# Optional: Set a display name or from address. If not set, SMTP_USER will be used.
SMTP_FROM="TPA Universitas <your-email@gmail.com>"

# Deliverability & performance tuning (optional)
# Enable SMTP connection pooling for faster multi-email sends
SMTP_POOL=true
# Verify transporter before send (set to false to shave latency once stable)
SMTP_VERIFY_BEFORE_SEND=true
# DKIM signing (recommended if using your own domain)
SMTP_DKIM_DOMAIN=""
SMTP_DKIM_SELECTOR="default"
SMTP_DKIM_PRIVATE_KEY=""
# TLS options
SMTP_TLS_REJECT_UNAUTHORIZED=false
SMTP_TLS_CIPHERS=""
SMTP_REPLY_TO=""

# Timezone Configuration
TZ=Asia/Jakarta
PGTZ=Asia/Jakarta

# On-Premise Mode
NEXT_PUBLIC_IS_ONPREM=false

# OTP blast test harness flags (safety)
# Must be true to allow running scripts/otp-blast.js
OTP_BLAST_ENABLE=false
# If base URL looks like production (vercel.app/.com), require explicit allow
OTP_BLAST_ALLOW_PROD=false